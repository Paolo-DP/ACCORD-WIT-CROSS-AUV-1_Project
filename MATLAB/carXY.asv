cartrans = transpose(car);
car2trans = transpose(car2);
x = cartrans(1,1:end);
y = cartrans(2,1:end);
i = [1:52];
plot(x,y, '-ob', 'linewidth', 3, 'markersize);
xlim([0 5200]);
ylim([0 5200]);

hold on;
s = size(car2trans);
i2 = [1:s(2)];
x = car2trans(1,1:end);
y = car2trans(2,1:end);
plot(x,y, '-or');

track = imread('track_5200x5200.jpg');
h = image(xlim,ylim,track); 
uistack(h,'bottom');

pbaspect([1 1 1]);

hold off;